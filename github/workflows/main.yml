name: Optimized CI with Caching

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      # Required identification step
      - name: 23f2003863@ds.study.iitm.ac.in
        run: echo "Workflow for Sachin Jha"

      # Step 1: Cache Setup
      - name: Cache dependencies
        id: cache-step
        uses: actions/cache@v4
        with:
          path: ~/.cache/pip
          key: cache-f0b8518

      # Step 2: The specific step that was missing
      - name: prime-cache-f0b8518
        run: echo "Cache hit status is ${{ steps.cache-step.outputs.cache-hit }}"
