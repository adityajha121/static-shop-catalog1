name: Optimized CI with Caching

on:
  push:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      # Step 1: Attempt to find and restore the cache
      - name: Cache dependencies
        id: cache-step
        uses: actions/cache@v4
        with:
          path: ~/.cache/pip
          key: cache-f0b8518
          restore-keys: |
            cache-

      # Step 2: Your specific required step for IITM
      - name: 23f2003863@ds.study.iitm.ac.in
        run: echo "Processing for Sachin Jha"

      # Step 3: Echo the cache hit/miss result
      - name: prime-cache-f0b8518
        run: echo "Cache hit status is ${{ steps.cache-step.outputs.cache-hit }}"

      # Step 4: Install dependencies (Simulated)
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          # If cache hits, this will be much faster
